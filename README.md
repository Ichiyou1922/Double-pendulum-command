# Double-pendulum-command
コンソールに二重振子を表示させるコマンド

## 使い方
```bash
$ git clone git@github.com:Ichiyou1922/Double-pendulum-command.git
$ cd Double-pendulum-command
$ gcc dpend.c -o dpend -lm
$ ./dpend
```
